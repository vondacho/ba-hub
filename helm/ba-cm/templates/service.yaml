apiVersion: v1
kind: Service
metadata:
  name: {{ include "ba-cm.fullname" . }}
  labels:
    {{- include "ba-cm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ba-cm.selectorLabels" . | nindent 4 }}
