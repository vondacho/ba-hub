apiVersion: v1
kind: Service
metadata:
  name: {{ include "ba-ddd-mapper.fullname" . }}
  labels:
    {{- include "ba-ddd-mapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ba-ddd-mapper.selectorLabels" . | nindent 4 }}
