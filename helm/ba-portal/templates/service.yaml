apiVersion: v1
kind: Service
metadata:
  name: {{ include "ba-portal.fullname" . }}
  labels:
    {{- include "ba-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ba-portal.selectorLabels" . | nindent 4 }}
